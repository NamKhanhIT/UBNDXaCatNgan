namespace Quanlycongviec.Domain.Enums
{
    public enum DocumentTypeEnum
    {
        QuyetDinh,   // Quyết định (QĐ)
        CongVan,    // Công văn (CV)
        ThongBao,   // Thông báo (TB)
        BaoCao,     // Báo cáo (BC)
        KeHoach,    // Kế hoạch (KH)
        ToTrinh,    // Tờ trình (TTr)
        CongDien    // Công điện (CĐ)
    }
}
